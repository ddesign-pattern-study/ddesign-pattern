package pattern.strategy.chan

class Air(
    var figureOfFindDust: Int = 500, // 미세먼지 수치
    var figureOfUltraFindDust: Int = 200, // 초미세먼지 수치
    var figureOfSmell: Int = 350, // 냄새 수치
    var humidity: Int = 40 // 습도
)